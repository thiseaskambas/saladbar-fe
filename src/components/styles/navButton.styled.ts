import styled from 'styled-components';

export const StyledNavButton = styled.button`
  border-radius: 0.5rem;
  padding: 0.5rem 1rem;
  font-weight: 300;

  display: flex;
  align-items: center;
`;
